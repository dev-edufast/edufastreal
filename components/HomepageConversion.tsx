import React from "react";

type HomepageConversionProps = {
  children?: React.ReactNode;
};

export function HomepageConversion({ children }: HomepageConversionProps) {
  return <>{children}</>;
}

export default HomepageConversion;
